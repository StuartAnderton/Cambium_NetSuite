/**
 * @NApiVersion 2.1
 */
define(['N/file', 'N/format', 'N/query', 'N/record', 'N/runtime'],
    /**
 * @param{file} file
 * @param{format} format
 * @param{query} query
 * @param{record} record
 * @param{runtime} runtime
 */
    (file, format, query, record, runtime) => {

        const foo = () => {

        }

        const bar = () => {

        }

        return {foo, bar}

    });
